# Copy to .env and set values locally.
# Admin username used in page.js for admin checks (see process.env.ADMIN).
ADMIN=your_admin_username

# Optional: override MongoDB URI in code later; for now page.js uses mongodb://localhost/ngo_app
